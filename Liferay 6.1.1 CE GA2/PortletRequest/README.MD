# javax.portlet.PortletRequest(Interface)

### 継承(extends)しているクラスリスト
- ClientDataRequest（Interface）：
- ActionRequest（Interface）：ActionをハンドルするためにPortletに送られるリクエスト
- ResourceRequest（Interface）：
- EventRequest（Interface）：
- RenderRequest（Interface）：